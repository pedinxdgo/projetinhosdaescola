/* var preco = 1500;
var imposto = 0.18;
var precoFinal = preco * (1 + imposto);
console.log(1500 * 0.18);
console.log(precoFinal);
 */

//console.log (19.9 * 0.6);

var preco = 19.9;
var desconto = 0.6;
var precoComDesconto = preco * (1 - desconto);
//console.log (precoComDesconto);

let nome = "Caderno";
let categoria = "Papelaria";
console.log("Produto: " + nome +  ", Preco: " + preco + ", Categoria: " + categoria + ", Desconto: " + desconto);